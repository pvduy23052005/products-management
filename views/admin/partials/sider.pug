.sider 
   .inner-menu    
      ul 
         li 
            a(href="/admin/dashboard" class = "btn" ) Tổng quan
         if(role.permissions.includes("products-category_view"))
            li    
               a(href="/admin/products-category" class = "btn") Danh mục sản phẩm
         if(role.permissions.includes("products_view"))
            li    
               a(href="/admin/products" class = "btn") Danh sách sản phẩm  
         if(role.permissions.includes("roles_view"))
            li 
               a(href="/admin/roles" class ="btn") Nhóm quyền
         if(role.permissions.includes("roles_permissions"))
            li 
               a(href ="/admin/roles/permissions" class = "btn") Phân quyền 
         if(role.permissions.includes("accounts_view"))
            li 
               a(href ="/admin/accounts" class = "btn" ) Danh sách tài khoản